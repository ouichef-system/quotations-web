import { createTheme } from "@mui/material"

export const ouiChefTheme = createTheme({
    palette: {
        primary: {
            main: '#24619f'
        },
        secondary: {
            main: '#000'
        }
    }
})